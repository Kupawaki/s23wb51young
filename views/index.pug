extends layout

block content
  h1= title
  p Welcome to #{title}
  p Did you know that there are at least 4000 ants on planet Earth?
  button(type="button", class="btn btn-primary" onclick="doReduce()") Press Me
  div(id="reduce") Reduce the value

  script.
    let count=20
    let change=-1

    function doReduce()
    {
      count += change
      change -= 1
      document.getElementById("reduce").innerHTML = `Count ${count} with change ${change}.`
    }